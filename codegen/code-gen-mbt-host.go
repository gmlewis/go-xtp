package codegen

import "errors"

// genMbtHostSDK generates Host SDK code to call the extension plugin in Mbt.
func (c *Client) genMbtHostSDK() (GeneratedFiles, error) {
	return nil, errors.New("mbt host Extism SDK does not yet exist")
}
